# Bighub-ai Tap

## How do I install these formulae?

`brew install bighub-ai/tap/<formula>`

Or `brew tap bighub-ai/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "bighub-ai/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
